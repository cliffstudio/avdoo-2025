<div class="universe-grid">		

	{% set sizeClasses = ['size-01', 'size-02', 'size-03', 'size-04', 'size-05', 'size-06'] %}
	{% set speedClasses = ['0.9', '0.85', '1.05'] %}
	
	{% for universePost in universePosts %}
	
		{% set image = universePost.imageModule.one() %}
		{% set orientationClass = (image.width >= image.height) ? 'landscape' : 'portrait' %}
		{% set randomSizeClass = random(sizeClasses) %}
		{% set randomSpeedAttr = random(speedClasses) %}
		{% set category = universePost.universeCategory.one() %}
		{% set postType = universePost.universePostType %}
		
		<div class="universe-block {{ orientationClass }} {{ randomSizeClass }}{% if category %} {{ category.slug }}{% endif %}{% if postType == 'link' %} link-post-type{% elseif postType == 'noClick' %} no-click-post-type{% else %} popup-post-type{% endif %}" data-universePost-id="{{ universePost.id }}" data-speed="{{ randomSpeedAttr }}" data-url="{{ universePost.url }}">
			
			{# Start Media #}
			<div class="media-wrap">
				{% include '00_site-wide/image-module_regular.twig' %}
			</div>
			{# End Media #}
			
			{# Start Captions #}
			<div class="caption-overlay">
				<div class="caption">
					{% if universePost.title|length %}
						<h3 class="size-02">{{ universePost.title }}</h3>
					{% endif %}
					{% if universePost.subtitle|length %}
						<h3 class="size-02">{{ universePost.subtitle }}</h3>
					{% endif %}
					{% if universePost.credit|length %}
						<h3 class="size-02">{{ universePost.credit }}</h3>
					{% endif %}
				</div>
			</div>
			{# End Captions #}
			
			{# Start Link #}
			{% if postType == 'link' %}
				{% set linkPostLink = universePost.universeLink %}
				{% if linkPostLink|length %}
					
					<a class="link" href="{{ linkPostLink.url }}" target="{{ linkPostLink.target }}" rel="noopener noreferrer"></a>
					
				{% endif %}
			{% endif %}
			{# End Link #}
			
		</div>
		
	{% endfor %}

</div>